#include <stdio.h>

int main() {
int linha, coluna;
    {for( linha = 1; linha < 24; linha ++)
        for( coluna = 1; coluna < 40; coluna ++)
    printf("-");
    putchar('\n');
    }
    return 0;
}
